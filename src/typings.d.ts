/**
 * TypeScript 全域型別定義檔案
 *
 * 此檔案用於：
 * - 為第三方函式庫提供型別定義
 * - 當 @types/ 套件不可用時的備用方案
 * - 手動添加全域型別宣告
 * - 擴展現有型別定義
 *
 * 遵循 TypeScript 嚴格模式規範，避免使用 any 型別
 */

// # 3rd Party Library
// If the library doesn't have typings available at `@types/`,
// you can still use it by manually adding typings for it
